Persistent Hashmap with predictable behavior, O(1) operations based on index over sequence files.

High writes throughput thanks to append-only logs and in-memory store, high reads throughput thanks to hash indexing.
